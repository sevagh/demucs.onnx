# Minimal dependencies needed to export Demucs to ONNX/ORT and run the C++ CLI.
# Keep this list lean to avoid platform-specific wheel issues (e.g. llvmlite/numba).
torch
numpy
-e ./demucs-for-onnx
onnxruntime
onnx
bin2c

# Optional analysis/evaluation dependencies (not required for conversion/build):
# librosa
# museval
# musdb
